@{
    ViewData["Title"] = "Categories Page";
}

@model List<Category>

<h2>All Categories</h2>
<div class="d-flex w-100 justify-content-end">
    <a asp-controller="category" asp-action="add" class="btn btn-success">+ Add new</a>
</div>

<table class="table table-striped">
  <thead>
    <tr>
      <th scope="col">Id</th>
      <th scope="col">Name</th>
      <th scope="col">Action</th>
    </tr>
  </thead>
  <tbody>
    @foreach (var item in Model)
    {
        <tr>
          <td style="width: 400px;">@item.Id</td>
          <td>@item.Name</td>
          <td>
              <div>
                  <a asp-controller="category" asp-action="edit" asp-route-id="@item.Id" class="btn btn-primary" style="width: 100px;">Edit</a>
                  <a asp-controller="category" asp-action="delete" asp-route-id="@item.Id" onclick="return confirm('Are you sure delete this news?');" class="btn btn-danger" style="width: 100px;">Delete</a>
              </div>
          </td>
        </tr>
    }
  </tbody>
</table>